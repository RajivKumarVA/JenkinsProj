pipeline {
  agent {
    label 'Slave2-deploy'
  }
  
  stages {
    stage ('Git Checkout') {
      steps {
        echo "Checkout from Git"
  //      git clone 'https://github.com/RajivKumarVA/JenkinsProj.git'
      }
    }
 
    stage ('Deploy') {
      steps {
//        echo "Deploying web page"
//        sudo chmod 777 deployWeb.sh
//        sh /home/ubuntu/deployWeb.sh
      }
    }
  }
}
